task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from 'target/docs/javadoc'
}

task sourcesJar(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

// build.dependsOn javadocJar, sourcesJar